<%= turbo_frame_tag dom_id(@schedule) do %>
  <%= form_with(model: @schedule, id: dom_id(@schedule), url: project_schedule_path(@project, @schedule)) do |form| %>
    <li class="flex py-4 border-b border-gray-200">
      <div class="flex w-full ml-3">
        <%= form.text_field :occupancy, class: "w-full block w-full shadow-sm focus:ring-light-blue-500 focus:border-light-blue-500 sm:text-sm border-gray-300 rounded-md"%>
        <div class="flex">
          <%= form.submit "Save", class:"btn-blue ml-3"%>
        </div>
        <div>
        </div>
      </div>
    </li>
  <% end %>
<% end %>
