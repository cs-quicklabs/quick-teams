<main class="pb-10 mx-auto max-w-7xl lg:py-12 lg:px-8">
  <div class="lg:grid lg:grid-cols-12 lg:gap-x-5">
    <%= render "shared/archive_menu" %>

    <main class="max-w-xl px-4 pb-12 lg:col-span-8">
      <div class="overflow-hidden bg-white">
        <div class="pb-4 ml-4">
          <%= render "shared/form_header", title: "Archived Projects", subtitle: @projects.load.size > 0 ? "Following projects have been archived." : "No record found." %>
        </div>

        <% if @projects.load.size > 0 %>
          <div class="flex space-x-4 ml-4">
            <div
              class="flex-1 min-w-0"
              data-behavior="autocomplete"
              data-controller="nav-search"
              data-nav-search-url-value="/search/archived"
            >
              <label for="search" class="sr-only">Search</label>

              <div class="relative rounded-md shadow-sm">
                <input
                  type="search"
                  name="search"
                  id="search"
                  class="pl-10 form-text-field"
                  placeholder="Search"
                  data-nav-search-target="input"
                  autocomplete="off"
                >
              </div>

              <div class="relative">
                <div
                  class="
                    absolute left-0 z-10 block w-full mt-1 origin-top-right bg-white rounded-md shadow-lg
                    focus:outline-none
                  "
                  role="menu"
                  aria-orientation="vertical"
                  aria-labelledby="options-menu"
                >
                  <ul data-nav-search-target="results"></ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <div class="overflow-hidden bg-white" data-controller="infinite-scroll">
          <div>
            <table class="table pt-4 border-separate">
              <thead class="bg-gray-50"></thead>

              <tbody class="table-body" id="projects" data-infinite-scroll-target="entries">
                <%= render partial: "projects/archived_project", collection: @projects, as: :project, cached: true %>
              </tbody>
            </table>

            <div class="flex justify-end invisible mt-2 mb-6 mr-2" data-infinite-scroll-target="pagination">
              <%== render partial: "shared/paginator", locals: { pagy: @pagy } %>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>
</main>
