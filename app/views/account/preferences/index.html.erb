<main class="pb-10 mx-auto max-w-7xl lg:py-12 lg:px-8">
  <div class="lg:grid lg:grid-cols-12 lg:gap-x-5">
    <%= render "shared/account_menu" %>

    <div class="max-w-xl px-4 lg:col-span-6">
      <%= render "shared/form_header", title: "Account Preferences", subtitle: "Setup your account preferences." %>

      <ul class="mt-2 divide-y divide-gray-200">
        <div id="account-email-notification">
          <%= render partial: "account/preferences/email_notification", locals: { account: @account } %>
        </div>
      </ul>

      <%= render @preferences %>
    </div>
  </div>
</main>
