<%= turbo_frame_tag dom_id(@job) do %>
  <%= form_with(model: @job, id: dom_id(@job), url: account_job_path(@job)) do |form| %>
    <li class="flex py-4 border-b border-gray-200">
      <div class="flex w-full">
        <%= form.text_field :name, class: "form-text-field"%>
        <div class="flex">
          <%= form.submit "Save", class:"btn-blue ml-3"%>
        </div>
        <div>
        </div>
      </div>
    </li>
  <% end %>
<% end %>
