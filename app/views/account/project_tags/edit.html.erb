<%= turbo_frame_tag dom_id(@project_tag) do %>
  <%= form_with(model: @project_tag, id: dom_id(@project_tag), url: account_project_tag_path(@project_tag)) do |form| %>
    <li class="flex py-4 border-b border-gray-200">
      <div class="flex w-full">
        <%= form.text_field :name, class: "form-text-field"%>
        <div class="flex">
          <%= form.submit "Save", class:"btn-blue ml-3"%>
        </div>
        <div>
        </div>
      </div>
    </li>
  <% end %>
<% end %>
