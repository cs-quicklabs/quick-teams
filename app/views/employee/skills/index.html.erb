<% presenter = UserPresenter.new(@employee) %>
<main class="relative z-0 flex-1 overflow-y-auto focus:outline-none">
  <%= render "employee/title", employee: @employee%>
  <main class="pb-8">
    <div class="px-4 sm:px-6" id="skills">
      <%= render partial: "form", locals: {skills: @skills, employee: @employee, skill: @skill}, cached: true %>
    </div>
  </div>
</main>
</main>
