<div class="px-4 py-4 sm:flex sm:items-center sm:justify-between sm:px-6 lg:px-8">
  <div class="flex flex-wrap items-baseline -mt-2 -ml-2">
    <h1 class="text-3xl font-bold leading-tight text-gray-900">
      <%= employee.display_name %>
    </h1>
    <p class="mt-1 ml-2 text-sm text-gray-500 truncate"><%= employee.display_position%></p>
  </div>
  <div class="flex mt-4 sm:mt-0 sm:ml-4">
    <div>
      <%= link_to 'Edit', edit_employee_path(employee), class:"btn-normal" %>
    </div>
    <% if employee.active %>
      <div>
        <%= link_to 'Deactivate', deactivate_user_path(employee), class:" ml-4 btn-normal" %>
      </div>
    <% else %>
      <div>
        <%= link_to 'Activate', activate_user_path(employee), class:" ml-4 btn-normal" %>
      </div>
    <% end %>
  </div>
</div>
<%= render 'employee/tabs'%>
</div>
