<%@nuggets = @nuggets.decorate %>
   <main class="max-w-4xl px-4 pt-10 pb-12 mx-auto space-y-6 lg:pb-16">
  <div class="px-4 py-4 sm:flex sm:items-center sm:justify-between sm:px-6 lg:px-8" id="milestone-detail">
    <%= render partial: "shared/nuggets/title", locals: {goal: @nugget}%>
    <div class="flex mt-4 sm:mt-0 sm:ml-4">
        <%= link_to 'Back','javascript:history.back()', class:"btn-normal" %>
        <%if !@nuggets.read? %>
        <div class="ml-3 p-2 rounded-md bg-blue-100">
             <p class="mt-3 text-sm md:mt-0 md:ml-2">
              <button type="button" data-reflex="click->Nugget#read" data-nugget-id="<%= @nuggets.id%>" data-employee-id="<%= current_user.id %>" class="font-medium text-blue-700 cursor-pointer whitespace-nowrap hover:text-blue-600">Mark as read </button>
            </p>
            </div>
      <% end %>
    </div>
  </div>
    <div class="mt-1 text-sm text-gray-900 sm:px-6 lg:px-8">
      <p><%= @nuggets.body%></p>
    </div>
</main>