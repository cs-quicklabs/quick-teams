<main class="relative z-0 flex-1 overflow-y-auto focus:outline-none">
  <%= render "employee/title", employee: @employee %>
  <div class="px-4 sm:px-6">
    <!-- Main 2 column grid -->
    <div class="grid items-start grid-cols-1 lg:grid-cols-3">
      <!-- Left column -->
      <div class="grid grid-cols-6 gap-2">
        <div class="ml-8 mt-8 mb-8">
          <dl class="divide-y divide-gray-200">
            <div class="py-4 sm:grid sm:py-5 sm:grid-cols-3 sm:gap-4">
              <dd class="mt-1 flex text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                <span class="w-48 text-sm font-medium text-gray-500">Email address</span>
                <span class="w-64 flex-grow"><%= @employee.email %></span>
                <span class="w-64 ml-4"></span>
              </dd>
            </div>
            <div class="py-4 sm:grid sm:py-5 sm:grid-cols-3 sm:gap-4">
              <%= render "inline_edit", model: @employee, attribute: :experience %>
            </div>
            <div class="py-6 sm:grid sm:py-5 sm:grid-cols-3 sm:gap-4">
              <%= render "inline_edit", model: @employee, attribute: :about %>
            </div>
            <div class="py-4 sm:grid sm:py-5 sm:grid-cols-3 sm:gap-4">
              <%= render "inline_edit", model: @employee, attribute: :cv %>
            </div>
          </dl>
        </div>
      </div>
    </div>
  </div>
</main>
